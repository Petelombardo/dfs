echo "Deploying Server"
for i in $(seq 1 5); do ssh root@gluster$i systemctl stop dfs-server; scp target/release/dfs-server target/release/dfs-admin root@gluster$i:/usr/bin/; ssh root@gluster$i systemctl start dfs-server; done

echo "Deploying Client"
ssh root@nanopir3 killall dfs-client
scp target/release/dfs-client target/release/dfs-admin root@nanopir3:/usr/bin/


