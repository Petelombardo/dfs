# Debugging scripts
debug_sqlite.sh
test_sqlite_sync.sh
deploy-to-clients
deploy-to-servers

# Deployment scripts (contain staging hostnames/paths)
deploy-build
build-x86.sh
dist/

# Log files
*.log
/tmp/

# Build artifacts (was tracked by mistake — every cargo build dirtied the
# working tree with ~10GB of binaries/fingerprints)
/target/

# Local Claude Code settings/state (machine-specific, not shared)
/.claude/
